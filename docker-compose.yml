services:
  backend:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: asset-tracker-backend
    ports:
      - '8080:8080'
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/asset_tracker?useSSL=true&enabledTLSProtocols=TLSv1.2,TLSv1.3
      SPRING_DATASOURCE_USERNAME: 3AanjvP5Xvf4WV8.root
      SPRING_DATASOURCE_PASSWORD: fyYhwZB13Gs85LOU
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      SPRING_JPA_HIBERNATE_DDL_AUTO: update

  frontend:
    build:
      context: ./ui
      dockerfile: Dockerfile
    container_name: asset-tracker-frontend
    ports:
      - '80:80'
    depends_on:
      - backend
